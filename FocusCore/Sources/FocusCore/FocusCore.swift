import Foundation

/// FocusCore is the shared Swift package containing models, protocols,
/// mock implementations, and utilities used by the Focus app and all extensions.
public enum FocusCore {
    /// The App Group identifier used for cross-extension data sharing.
    public static let appGroupIdentifier = "group.com.colbychang.focus.shared"

    /// The current version of the FocusCore package.
    public static let version = "1.0.0"
}
